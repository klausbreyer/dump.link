import React from "react";
import FlexCol from "../common/FlexCol";

interface DagProps {
  // [key: string]: any;
}

const Dag: React.FC<DagProps> = (props) => {
  return <div className="mx-auto max-w-7xl sm:px-6 lg:px-8">Dag</div>;
};

export default Dag;
