import React from "react";
import FlexCol from "../common/FlexCol";

interface GraphProps {
  // [key: string]: any;
}

const Graph: React.FC<GraphProps> = (props) => {
  return <div className="mx-auto max-w-7xl sm:px-6 lg:px-8">Graph</div>;
};

export default Graph;
